# Rust
target/
Cargo.lock
**/*.rs.bk

# Solana
.anchor/
test-ledger/
.DS_Store

# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Logs
*.log

# Build artifacts
dist/
build/

# Temporary files
*.tmp
*.temp

# Solana CLI
~/.config/solana/

# Anchor
.anchor/
test-ledger/
.DS_Store

# Program IDs (keep these local)
Anchor.toml.local

# Bot testing wallets (keep private!)
bots/wallets/*.json
bots/registry.json
bots/funding-status.json
bots/results/*.json# Secrets and private keys (NEVER commit!)
SECRETS.md
**/secrets/**
**/*.key
**/*.pem
**/*.p12
**/*.pfx
*.keypair
*.secret# Test wallets (keep private!)
test-wallets/
test-wallets/**/*.json
wallet-backups/
wallet-backups/**/*
speed-run-wallets/
speed-run-wallets/*.json

# Testnet keys (NEVER commit!)
testnet-key.json
**/testnet-key.json
**/*-key.json
**/*keypair*.json
pball-new-keypair.json

# Internal / personal docs (do not publish)
GET_DEVNET_SOL.md
DEVNET_DEPLOYMENT_SUMMARY.txt
VERCEL_V2_ENV.txt
jj.md
docs/ADD_OLD_WALLET*
docs/HOW_FjbPun*
docs/HOW_WHY_FjbPun*
docs/JACKPOT_*
docs/MOVE_*
docs/NEVER_SEND*
docs/SAFE_NEW_KEYPAIR*
docs/SEARCH_CHAT*
docs/TEST_DEPLOY_BEFORE*
docs/WHAT_HAPPENED*
docs/WHERE_VAULT*
token-lp-info.json
bots/addresses.txt
bots/wallets/*.txt
bots/results/