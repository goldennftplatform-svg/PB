# One-time: add these in Vercel Dashboard → your project → Settings → Environment Variables
# Production: set VITE_ENV=LIVE. Preview: PREVIEW is ok.
# Then redeploy.
#
# If the site asks you to "login to Vercel": that's Vercel Deployment Protection, not our app.
# Turn it OFF: Project → Settings → Deployment Protection → disable Vercel Authentication
# (and Password Protection) for Production/Preview so the deployment is public.

VITE_TAROBASE_APP_ID=697010c4c6f87869899205a3
# Use LIVE for Production so app is not in dev/preview mode. PREVIEW ok for preview deployments.
VITE_ENV=LIVE
# solana_mainnet = wallet connect works with Phantom/mainnet. Use offchain only for full mock.
VITE_CHAIN=solana_mainnet
VITE_RPC_URL=https://celestia-cegncv-fast-mainnet.helius-rpc.com
VITE_WS_API_URL=wss://api.tarobase.com/ws/v2
VITE_API_URL=https://api.tarobase.com
VITE_AUTH_API_URL=https://auth.tarobase.com

# Wallet (Privy) — App ID below. If you get "Could not log in with wallet":
# 1. In Privy Dashboard (dashboard.privy.io) → your app → Allowed domains: add your exact site URL (e.g. https://your-app.vercel.app) and https://localhost:5173 for local.
# 2. Keep VITE_CHAIN=solana_mainnet so Phantom (mainnet) can connect.
VITE_PRIVY_APP_ID=cmmdygahu007n0cl7vqdv5cpy
# IMPORTANT: Privy app SECRET is for server-side only. If you need it (e.g. backend auth),
# add it in Vercel as PRIVY_APP_SECRET (no VITE_ prefix) and never commit or expose it.
# Optional: use your own Privy API (self-hosted or custom domain). Default is Privy cloud.
# VITE_PRIVY_API_URL=https://auth.privy.io
# Optional: Phantom deep link app ID
# VITE_PHANTOM_APP_ID=
# Optional: force auth method (default is privy)
# VITE_AUTH_METHOD=privy
